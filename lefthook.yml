pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{kt,kts}"
      exclude: "src/generated/**"
      run: bin/kotlin-format --set-exit-if-changed {staged_files}
      stage_fixed: true
